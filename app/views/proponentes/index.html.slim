.card.mt-2.mb-3
  .card-header
    .row.align-items-center
      .col-6
        h3 Proponentes

      .col-6
        .float-end= link_to 'Novo proponente', new_proponente_url, class: 'btn btn-success'

.card
  .card-body
    table.table.table-striped.table-resp
      thead 
        tr
          th CPF
          th Nome
          th Data nascimento
          th Endereço
          th Contatos

      tbody
        - @proponentes.each do |proponente|
          tr
            td= proponente.cpf
            td= proponente.nome
            td= proponente.data_nascimento
            td= proponente.enderecos.map(&:nome).to_sentence
            td= proponente.contatos.map(&:telefone).to_sentence
